import { atom } from 'jotai';

export const isLoaderRunningAtom = atom(false);
export const isSceneLoadedAtom = atom(false);
export const serialPortAtom = atom(null);
export const potentiometerPositionAtom = atom(null);
